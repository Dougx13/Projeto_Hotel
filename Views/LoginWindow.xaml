<Window x:Class="HotelAdminWpf8.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - Sistema de Hotel" 
        WindowStartupLocation="CenterScreen" Background="#1E1E2F">
    <Grid>
        <Border CornerRadius="12" Background="#2B2B3D" Margin="50" Padding="24">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Sistema de Reserva de Hotel" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBox x:Name="txtUsuario" Margin="0,0,0,8" Padding="8" Height="30" Width="432"/>
                <PasswordBox Name="txtSenha" Margin="0,0,0,16" Padding="8" Height="27" Width="433" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0" >
                    <Button Content="Entrar como Admin" Width="160" Height="36" Margin="6" Click="BtnAdmin_Click" Background="#4C8BF5" Foreground="White"/>
                    <Button Content="Entrar como Cliente" Width="160" Height="36" Margin="6" Click="BtnCliente_Click" Background="#52C45E" Foreground="White"/>
                    <Button Content="Cadastrar-se"
        Width="207"
        Height="42"
        Background="#3B3B50"
        Foreground="White"
        BorderBrush="Transparent"
        FontWeight="Bold"
        Cursor="Hand"
        Click="BtnCadastrar_Click"/>

                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>

